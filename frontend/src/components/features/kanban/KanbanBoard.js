/**
 * KanbanBoard
 * Direct re-export of KanbanBoardContainer.
 * Import from here so callers don't depend on the internal folder structure.
 */
export { default } from './containers/KanbanBoardContainer';
